## Overview
Server-side template injection checks whether user input is evaluated by a template engine on the server.

## Steps
- Identify reflected inputs in template-rendered pages.
- Start with arithmetic probes.
- Fingerprint the template engine safely.
- Avoid command execution unless explicitly authorized.
- Document path from input to server-side evaluation.

## Evidence
Capture payload, response, engine indicators, and maximum approved impact proof.
